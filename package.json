{
  "name": "AllPrices",
  "author": "EstefanoC",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "description": "Page to compare prices of any products",
  "scripts": {
    "client:install": "cd frontend && npm install",
    "client:build": "cd frontend && npm build",
    "client": "cd frontend && npm run start",
    "dev": "concurrently \"npm run client\" \"npm --version\""
  },
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
